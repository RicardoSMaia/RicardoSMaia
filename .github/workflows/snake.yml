name: Test Token

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test token
        run: |
          echo "Token exists: ${{ secrets.SNAKE_TOKEN: }}
          echo "Token length: ${{ secrets.SNAKE_TOKEN: }}"
        env:
          TOKEN: ${{ secrets.SNAKE_TOKEN }}
